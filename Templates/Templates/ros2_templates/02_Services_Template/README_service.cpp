// Services are Anonymous: a client does not know which node is behind the service, it just calls the service. And the server does not know which nodes are clients, it just receives requests and responds to them.

// --- is used to seperate request from response data types/messages in ros2 documentation for interfeaces.